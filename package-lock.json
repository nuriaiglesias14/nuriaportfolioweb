/* =========================================================
   Nuria Iglesias — sistema de diseño
   ========================================================= */

:root{
  /* color */
  --paper:#F5F3EC;
  --paper-2:#EDEAE1;
  --ink:#1C1F2A;
  --gray:#5F5F58;
  --line:#D8D4C9;
  --blue:#6F7E9B;
  --blue-ink:#414E70;
  --blue-wash:#DDE1EA;

  /* tipografía */
  --display:"Playfair Display",Georgia,serif;
  --sans:"Inter",-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
  --mono:"JetBrains Mono",ui-monospace,SFMono-Regular,Menlo,monospace;

  /* espaciado */
  --s1:8px; --s2:16px; --s3:24px; --s4:40px; --s5:64px; --s6:96px;
  --sec:clamp(96px,10.5vw,176px);          /* padding vertical de sección */
  --pad:clamp(22px,4.4vw,72px);            /* padding lateral */
  --gut:clamp(16px,1.8vw,28px);            /* gutter de retícula */
  --max:1440px;

  --ease:cubic-bezier(.22,.7,.2,1);
}

*,*::before,*::after{box-sizing:border-box}
html{scroll-behavior:smooth;-webkit-text-size-adjust:100%}
body{
  margin:0;background:var(--paper);color:var(--ink);
  font-family:var(--sans);font-size:16px;line-height:1.7;font-weight:400;
  -webkit-font-smoothing:antialiased;overflow-x:hidden;
}
img{display:block;max-width:100%}
a{color:inherit}
p{margin:0 0 1em}
p:last-child{margin-bottom:0}
button{font:inherit}
:focus-visible{outline:2px solid var(--blue-ink);outline-offset:4px}
::selection{background:var(--blue-wash);color:var(--ink)}

/* ---------- layout ---------- */
.wrap{max-width:var(--max);margin:0 auto;padding-inline:var(--pad)}
.g{display:grid;grid-template-columns:repeat(12,1fr);gap:var(--gut)}
section{padding-block:var(--sec)}

@media(max-width:1024px){ .g{grid-template-columns:repeat(8,1fr)} }
@media(max-width:640px){  .g{grid-template-columns:repeat(4,1fr)} }

/* ---------- tipografía ---------- */
.eyebrow{
  font-family:var(--mono);font-size:11px;font-weight:400;letter-spacing:.13em;
  text-transform:uppercase;color:var(--gray);margin:0;
}
.meta{
  font-family:var(--mono);font-size:11px;letter-spacing:.11em;
  text-transform:uppercase;color:var(--gray);margin:0;
}
h1,h2,h3{font-family:var(--display);font-weight:400;margin:0;letter-spacing:-.016em}
h1{font-size:clamp(31px,3.5vw,52px);line-height:1.1}
h2{font-size:clamp(30px,3.5vw,52px);line-height:1.1}
h3{font-size:clamp(24px,2.6vw,38px);line-height:1.1}
em{font-style:italic}
.lede{font-size:clamp(15px,1.15vw,17px);line-height:1.75;color:var(--gray)}
.small{font-size:14.5px;line-height:1.75;color:var(--gray)}

/* ---------- botón editorial ---------- */
.btn{
  display:inline-flex;align-items:center;gap:12px;
  font-family:var(--mono);font-size:11px;font-weight:400;
  letter-spacing:.11em;text-transform:uppercase;text-decoration:none;
  background:var(--blue-ink);color:var(--paper);
  border:1px solid var(--blue-ink);border-radius:0;
  padding:18px 34px;cursor:pointer;position:relative;overflow:hidden;
  transition:color .4s var(--ease),border-color .4s var(--ease);
}
.btn::before{
  content:"";position:absolute;inset:0;background:var(--ink);
  transform:scaleX(0);transform-origin:right;
  transition:transform .5s var(--ease);z-index:0;
}
.btn:hover::before,.btn:focus-visible::before{transform:scaleX(1);transform-origin:left}
.btn > *{position:relative;z-index:1}
.btn i{font-style:normal;transition:transform .4s var(--ease)}
.btn:hover i{transform:translateX(5px)}

.btn-ghost{
  background:transparent;color:var(--blue-ink);border-color:var(--blue);
}
.btn-ghost::before{background:var(--blue-ink)}
.btn-ghost:hover,.btn-ghost:focus-visible{color:var(--paper);border-color:var(--blue-ink)}

/* ---------- panel ---------- */
.panel{background:var(--paper-2);padding:clamp(28px,2.8vw,48px)}

/* ---------- figura, duotono y texto en hover ---------- */
.card{position:relative;display:block}
.fig{
  position:relative;overflow:hidden;background:var(--paper-2);
  width:100%;height:100%;
}
/* capa que se mueve con el scroll (parallax) */
.fig-move{
  position:absolute;left:0;width:100%;
  top:-7%;height:114%;
  will-change:transform;
}
.fig img{
  width:100%;height:100%;object-fit:cover;
  filter:grayscale(1) contrast(1.03);
  transition:transform 1.2s var(--ease);
}
.card:hover .fig img{transform:scale(1.05)}
.tint{
  position:absolute;inset:0;background:var(--blue);
  mix-blend-mode:color;opacity:0;
  transition:opacity .6s var(--ease);pointer-events:none;
}
.veil{
  position:absolute;inset:0;background:var(--blue-ink);opacity:0;
  transition:opacity .6s var(--ease);pointer-events:none;
}

/* la capa de texto: sólo el enlace es clicable */
.on-img{
  position:absolute;inset:0;z-index:2;
  display:flex;align-items:center;justify-content:center;
  padding:var(--s4);pointer-events:none;
}
.on-img a{
  pointer-events:auto;text-decoration:none;color:#fff;text-align:center;
  display:flex;flex-direction:column;align-items:center;gap:14px;
  opacity:0;transform:translateY(14px);
  transition:opacity .5s var(--ease),transform .65s var(--ease);
}
.on-img .k{
  font-family:var(--mono);font-size:11px;font-weight:400;letter-spacing:.13em;
  text-transform:uppercase;opacity:.85;margin:0;
}
.on-img .t{
  font-family:var(--display);font-size:clamp(26px,3vw,46px);
  line-height:1.06;letter-spacing:-.012em;
  padding-bottom:6px;border-bottom:1px solid transparent;
  transition:border-color .4s var(--ease);
}
.on-img a:hover .t,.on-img a:focus-visible .t{border-color:rgba(255,255,255,.7)}

.card:hover .tint,.card:focus-within .tint{opacity:.95}
.card:hover .veil,.card:focus-within .veil{opacity:.55}
.card:hover .on-img a,.card:focus-within .on-img a{opacity:1;transform:none}

/* sin hover (táctil): el texto queda visible */
@media(hover:none){
  .on-img a{opacity:1;transform:none}
  .tint{opacity:.95}
  .veil{opacity:.5}
}

/* ---------- cursor personalizado ---------- */
.cursor{
  position:fixed;top:0;left:0;z-index:90;pointer-events:none;
  width:64px;height:64px;margin:-32px 0 0 -32px;border-radius:50%;
  background:var(--blue-ink);color:var(--paper);
  display:grid;place-items:center;
  font-family:var(--mono);font-size:9.5px;letter-spacing:.12em;text-transform:uppercase;
  opacity:0;transform:scale(.2);
  transition:opacity .35s var(--ease),transform .45s var(--ease);
}
.cursor.is-active{opacity:1;transform:scale(1)}
@media(hover:none),(max-width:640px){.cursor{display:none}}

/* ---------- animación de entrada ---------- */
[data-anim="title"] .word{display:inline-block;overflow:hidden;vertical-align:top;padding-bottom:.06em;margin-bottom:-.06em}
[data-anim="title"] .char{display:inline-block;will-change:transform}
[data-anim="read"] .word{display:inline-block;will-change:opacity}

/* sin JS o antes de inicializar, nada de saltos */
html:not(.no-js) [data-anim]{visibility:hidden}
.no-js [data-anim]{visibility:visible !important;opacity:1 !important;transform:none !important}
.no-js [data-anim]{opacity:1 !important;transform:none !important}

@media(prefers-reduced-motion:reduce){
  *{animation-duration:.01ms !important;transition-duration:.01ms !important}
  html{scroll-behavior:auto}
  .cursor{display:none}
}
